{
  "schemaVersion": "1.0.0",
  "projectStateVersion": 3,
  "summary": "New project",
  "architectural_notes": [],
  "known_issues": [
    "Admin registration flow returns 'Unauthorized: Only admins can assign user roles' error when trying to register the first admin, likely a chicken-and-egg auth guard issue"
  ],
  "isFixRequest": true,
  "existing_features": [],
  "new_feature_requests": [
    {
      "id": "AR-1",
      "summary": "Fix the chicken-and-egg authorization bug in the backend where registering the very first admin fails with 'Unauthorized: Only admins can assign user roles'. The `registerFirstAdmin` function (or equivalent initial admin setup function) must bypass the admin-only auth guard when no admins exist yet in the system. Add a condition: if the admin user count is zero, allow the registration to proceed without requiring an existing admin caller. If one or more admins already exist, the normal admin-only guard must apply.",
      "reqIds": [
        "REQ-1"
      ],
      "status": "pending"
    }
  ],
  "projectName": "Nellore Print Hub",
  "clarification_mode": "instant",
  "clarification_rounds": 0
}